[package]
name = "tabibu-salama"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false               # internal workspace crate, never published

# Salama LOCAL privacy engine — the half that needs no server and no Developer
# ID: read the machine's exposure (public IP / DNS), and generate a native
# macOS encrypted-DNS (DoH) configuration profile the user approves once. Pure
# parsers + profile generation are unit-tested; the OS reads shell out.

[dependencies]
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
