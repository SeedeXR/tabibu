<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Menu bar app (agent): no Dock icon, no app switcher entry. The runtime
	     twin is ActivationPolicy::Accessory in main.rs (covers `tauri dev`);
	     this covers the bundle from the very first frame after launch. -->
	<key>LSUIElement</key>
	<true/>
</dict>
</plist>
